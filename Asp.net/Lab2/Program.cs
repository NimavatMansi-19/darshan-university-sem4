using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading.Tasks;

namespace Lab2
{
    internal class Program
    {
        static void Main(string[] args)
        {
        //// Write a program to create a class named Candidate with ID, Name, Age, Weight and Height as data members &
        //// also create a member functions like GetCandidateDetails() and DisplayCandidateDetails().
        //Candidate c =new Candidate();
        //c.GetCandidateDetails();
        //c.DisplayCandidateDetails();

        //// Write a program to create a class Staff having data members as Name, Department, Designation, Experience & Salary.
        // //Accept this data for 5 different staffs and display only names & salary of those staff who are      HOD.
        // Staff[] s = new Staff[5];
        // for (int i = 0; i <= 1; i++)
        // {
        //     s[i] = new Staff();
        //     s[i].GetDetails();

        // }
        // for (int i = 0; i <= 1; i++)
        // {
        //     s[i].GetDesignation(s[i].Designation);

        // }

        //// Write a pogram to Create a class Bank_Account with Account_No,
        //// Email, User_Name, Account_Type and Account_Balance as data members.
        //// Also create a Member function GetAccountDetails() & DisplayAccountDetails().
        //Bank_Account account = new Bank_Account();
        //account.GetAccountDetails();
        //account.DisplayAccountDetails();

        ////Write a program with following specifications:
        ////Class Name: Student Data Members: Enrollment_No, Student_Name, Semester, CPI and SPI
        ////Get Students Details using constructor and DisplayStudentDetails() using member function. 
        //Student student = new Student();
        //student.DisplayStudentDetails();

        ////Write a program to calculate area of a Rectangle using constructor. 
        //Rectangle r = new Rectangle(3, 4);

        ////Write a program for implementing single inheritance which creates one class
        ////Account_Details for getting account information and another
        //// class Interest for  calculating and displaying total interest from the
        //// data inserted from account details.
        //Interest interest = new Interest();
        //interest.calculating();
        //interest.displaying();

        //// Write a program to Define a class Salary which will contain member variable
        //// Basic, TA, DA, HRA. Write a program using Constructor with default values
        //// for DA and HRA and calculate the salary of employee.
        //salary s = new salary(3000,4000);
        //s.CalculateTotalSalary();

        ////Write a program to Define a class Distance have data members dist1, dist2, dist3.
        ////Initialize the two data members using constructor and store their addition in third data member using
        ////function and display addition.
        //Distance d = new Distance(3.3, 4.4);
        //d.CalculateAddition();
        //d.DisplayAddition();

        //// Create a class Furniture with material ,price as data members. Create another class Table with Height,
        //// surface_area as data members. Write a program to implement single inheritance. 
        //Table table = new Table();

        //10. Program to implement the following multiple inheritance using interface 
        //Interface: Gross Method-Gross_sal()
        //Class: Salary Data Members - HRA, TA,DA Methods -Disp_sal()
        //Class: Employee Data Members - Nam

        }
    }
}
